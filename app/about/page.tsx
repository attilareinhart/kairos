export default function AboutPage() {
  return (
    <section className="max-w-3xl p-8">
      <p className="text-sm font-semibold uppercase tracking-[0.35em] text-[var(--accent)]">About</p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-[var(--ink)]">A polished foundation for future features.</h1>
      <p className="mt-4 text-lg leading-8 text-[var(--muted)]">
        This route demonstrates how the persistent drawer keeps navigation available while the main content changes smoothly.
      </p>
    </section>
  );
}
