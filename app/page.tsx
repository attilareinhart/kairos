export default function Home() {
  return (
    <main className="page-shell">
      <section className="hero-card" aria-labelledby="hello-world-heading">
        <p className="eyebrow">Kairos baseline app</p>
        <h1 id="hello-world-heading">Hello World!</h1>
        <p className="lede">
          This is a minimal Next.js starting point ready for future features.
        </p>
      </section>
    </main>
  );
}
