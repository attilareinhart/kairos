export default function Home() {
  return (
    <section className="max-w-3xl p-8">
      <p className="text-sm font-semibold uppercase tracking-[0.35em] text-[var(--accent)]">Home</p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-[var(--ink)]">A flexible starting point for your product.</h1>
      <p className="mt-4 text-lg leading-8 text-[var(--muted)]">
        The header and drawer now create a durable app shell, so future pages can build on top of a consistent navigation experience.
      </p>
    </section>
  );
}
