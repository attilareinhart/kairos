export default function ServicesPage() {
  return (
    <section className="max-w-3xl p-8">
      <p className="text-sm font-semibold uppercase tracking-[0.35em] text-[var(--accent)]">Services</p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-[var(--ink)]">Tailored experiences with a simple shell.</h1>
      <p className="mt-4 text-lg leading-8 text-[var(--muted)]">
        The new layout is built to support richer views, content sections, and future interactions without reworking the app frame.
      </p>
    </section>
  );
}
